<?php

// Comma separated lists of hostnames to resolve and add to the excluded IP data
$hostnames = array('');

// Comma separated lists of static IP addresses to add to the excluded IP data
$ip_addresses = array('');

// Database credentials
$db_host = "";
$db_name = "";
$db_username = "";
$db_password = "";

?>